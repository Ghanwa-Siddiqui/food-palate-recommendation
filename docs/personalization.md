# Personalization Engine

Module: **Personalization Engine** · Owner: **Manahil** · Branch: `feature/personalization-manahil`

Part of the Taste Engine 2-day sprint. This module owns the user taste
vector: building it from onboarding answers, updating it via interactions,
and exposing it to the ranking engine and feed UI.

## Sibling modules

| Owner   | Module                     | Branch                                |
|---------|----------------------------|---------------------------------------|
| Ganva   | Data Layer & Backend Core  | `feature/data-core-ganva`             |
| Ifreen  | Review Intelligence        | `feature/review-intelligence-ifreen`  |
| Manahil | Personalization Engine     | `feature/personalization-manahil`     |
| Esha    | Ranking Engine & Feed UI   | `feature/ranking-feed-esha`           |

## Quick start

```bash
python -m venv .venv
.\.venv\Scripts\activate         # or source .venv/bin/activate on macOS/Linux
pip install -r requirements.txt

python scripts/generate_mock_dishes.py   # writes data/mock_dishes.json
uvicorn app.main:app --reload
```

Then open <http://127.0.0.1:8000/onboarding> to fill the form, or
<http://127.0.0.1:8000/docs> for the interactive API.

Run the test suite:
```bash
pytest -q
```

## What's in this module

```
app/
  main.py               FastAPI entrypoint
  config.py             env-driven config
  models.py             Pydantic contracts (shared shapes)
  embedding.py          deterministic mock text embedding (128-dim)
  vector_math.py        cosine, EMA update, top-k similar
  personalization.py    onboarding -> vector, interaction -> EMA update
  dish_store.py         reads Ganva's dish fixtures (mock on Day 1)
  repositories/
    json_repo.py        Day-1 JSON-file store
    supabase_repo.py    Day-2 Supabase-backed store
  routers/
    onboarding.py       GET/POST /onboarding + /onboarding/done
    user.py             /api/user/{id}/taste-vector | interaction | similar
  templates/, static/   onboarding form UI
data/
  mock_dishes.json      Day-1 dish fixtures (regenerated by script)
  users.json            (created on first onboarding)
  interactions.json     (created on first interaction)
docs/
  contracts.md          public API contract for Esha
  personalization.md    this file
scripts/
  generate_mock_dishes.py
tests/                  pytest suite (unit + API)
```

## Configuration

Environment variables (see `.env.example`):

| Var                | Default | Meaning                                          |
|--------------------|---------|--------------------------------------------------|
| `STORAGE_BACKEND`  | `json`  | `json` (day 1) or `supabase` (day 2)             |
| `SUPABASE_URL`     | —       | required when backend is `supabase`              |
| `SUPABASE_KEY`     | —       | required when backend is `supabase`              |
| `VECTOR_DIM`       | `128`   | embedding dimensionality                         |
| `EMA_ALPHA`        | `0.15`  | how hard interactions nudge the taste vector     |

## API contract

See [contracts.md](contracts.md). Endpoints Esha's ranking engine
and feed UI call:

- `GET  /api/user/{user_id}/taste-vector`
- `GET  /api/user/{user_id}/interaction`
- `POST /api/user/{user_id}/interaction`
- `GET  /api/user/{user_id}/similar?k=5`

## Design notes

**Deterministic mock embedding.** `embed_text` seeds numpy's PRNG from a
SHA-256 hash of the (lowercased, stripped) input, then draws a unit-length
128-d vector. Same string in → same vector out, always. Averaging a bag of
term vectors gives us cheap "topics" — two Pakistani dishes score ~0.30
cosine, Pakistani vs Japanese scores ~0.03. Real semantic embeddings can
drop into `embed_text` unchanged; nothing else needs to know.

**EMA feedback loop.** When a user clicks/saves/orders a dish, we blend the
dish vector into the user vector with `α = EMA_ALPHA` (default 0.15) and
renormalize. Small α = slow drift, large α = jumpy taste profile.

**Storage abstraction.** `Repository` (Protocol) has two backends:
`JsonRepository` for local dev, `SupabaseRepository` for prod. Everything
above the storage layer sees only the interface. Swap with one env var.
